Set up automated downloading of data every minute + bg extraction

Set up automated sending/retrieval to Vision AIP

Test effectiveness of building own Vision ML model

WRITE LIT REVIEW FOR EACH SECTION OF MODEL

Read up on paper marc is using as justification for switching turtlebots to rpi

List justifications for each decision made 
	(e.g. using FTP for transfering images instead of HTTP or a Cloud Storage API)
	(e.g. 1 frame a minute instead of 1 frame every 1/32 seconds)